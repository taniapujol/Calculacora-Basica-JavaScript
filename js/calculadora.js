// Empezamos nuestro jquerry externo
$(document).ready(function(){
  // declaracion de variables globales
  var testeo = true;
  if (testeo) console.log ("jquery cargado");
  // Abrir ventana calculadora
  $("#abir").click(function(){
    $(".ventana").slideDown();
  });
  // cerrar ventana calculadora
  $("#cerrar").click(function(){
    $(".ventana").slideUp();
  });
  // Funcion obtener numero (Boton (0)..(9))

  // Funcion suma (Boton (+))

  // Funcion resta (Boton (-))

  // Funcion multiplicar (Boton (*))

  // Funcion dividir (Boton (/))

  // Funcion Borrar (Boton (C))

  // Funcion Borrar un numero (Boton (CE))

  // Funcion punto de decimal (Boton (.))

  // Funcion resultado (Boton (=))

  // Fin js
});
